<UserControl x:Class="MetroPlurk.Views.ProgressBar.ProgressView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:MetroPlurk.Views.ProgressBar"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    mc:Ignorable="d" d:DesignHeight="480" d:DesignWidth="620"
    >
    <Grid x:Name="LayoutRoot" Height="26" Background="{StaticResource PhoneAccentBrush}">
        <TextBlock x:Name="Message"
                   Text="Loading"
                   Margin="0"
                   HorizontalAlignment="Center"
                   FontSize="{StaticResource PhoneFontSizeSmall}" />
    </Grid>

</UserControl>
